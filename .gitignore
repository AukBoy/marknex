# Dependencies
node_modules/

# Environment / secrets
.env
.env.local

# Logs
*.log

# SQLite database (incl. WAL/SHM sidecars) & uploaded files (runtime data).
# Broad *.sqlite* guard so no DB with student PII / password hashes is committed.
backend/marknex.db
backend/marknex.db-wal
backend/marknex.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm
backend/uploads/

# Generated report (binary doc, not source)
MarkNex_System_Report.doc

# Scratch / debug output
backend/out.json
backend/out.txt
backend/db_dump.json

# Build output
frontend/dist/
frontend/node_modules/.vite/

# Local dev tooling
.claude/launch.json

# Unrelated sub-project — not part of the deploy
youtube-channel-switcher/
