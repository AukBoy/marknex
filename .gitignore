# Dependencies
node_modules/

# Environment / secrets
.env
.env.local

# Logs
*.log

# SQLite database (incl. WAL/SHM sidecars) & uploaded files (runtime data).
# Broad *.sqlite* guard so no DB with student PII / password hashes is committed.
backend/marknex.db
backend/marknex.db-wal
backend/marknex.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm
backend/uploads/

# Generated reports & DB exports — contain REAL student PII / password hashes.
# The GitHub repo is PUBLIC, so these must never be committed.
MarkNex_System_Report.doc
MarkNex_Database_Report.docx
MarkNex_Database_Report.pdf
db-report-data.json
db-report-preview.png
db-screenshots/
# Local one-off report generators (reference the live DB; not part of the app)
backend/gather-db-report.js
backend/build-db-report.js
backend/build-db-pdf.js
backend/build-db-screenshots.js

# Scratch / debug output
backend/out.json
backend/out.txt
backend/db_dump.json

# Build output
frontend/dist/
frontend/node_modules/.vite/

# Local dev tooling
.claude/launch.json

# Unrelated sub-project — not part of the deploy
youtube-channel-switcher/
